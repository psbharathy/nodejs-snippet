// Regulrar Javascript

function sayHello(name) {
  console.log("Hello " + name);
}

sayHello("Bharathy");

// Window is not defined
// console.log(window);
// console is glbal object
// other global objects are => global.console.log
// global.setInterval()

// in Node every file is a module
// inside file are scoped to that file
console.log(module);
